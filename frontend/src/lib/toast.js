// Ek jagah se saara app "toast" import kare — taaki har success/error alert
// consistent style/timing mein dikhe, alert()/window.confirm() ki jagah
import { toast } from "react-toastify";

export { toast };
